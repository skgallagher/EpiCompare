# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r
repos:
  CRAN: https://cloud.r-project.org

r_packages:
  - covr
  - kableExtra

install:
  - R -e 'install.packages("devtools")'
  - R -e 'devtools::install_github("Mr8ND/TC-prediction-bands/TCpredictionbands", upgrade = "never")'
  - R -e 'devtools::install_github("ngloe/olpsR", upgrade = "never")'
  - R -e 'devtools::install_deps(dep = T, upgrade = "never")'

script:
- R CMD build .
- R CMD check *tar.gz


matrix:
  include:
  - r: release
    name: tidyr-old
    before_install: R -e 'install.packages("tidyr", version = "0.8.3")'

after_success:
- R -e 'covr::codecov()'

